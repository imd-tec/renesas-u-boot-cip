/* Literals, not CONFIG_DEFAULT_FDT_FILE: the CONFIG string expands with its
 * quotes intact, which end up inside the environment value.
 */
fdtfile=imdt-v2h-sbc.dtb
serial#=imdt-v2h-sbc

#include <env/imdt/v2-sbc.env>
